type Gender is enum
     Male;
     Female;
end

type Person is record
     name: str;
     age: i32;
     gender: Gender;
end

fn read(filename: str) -> i32
   let nathan = .{
          name: "nathan",
          age: 17,
          gender: Male,
   } as Person;

   let nathan: Person = .{
       name: "nathan",
       age: 17,
       gender: Male,
   };

   const name = filename;

   const a = (25 * 2) as i32;
   const b = a * 2;

   return b;
end