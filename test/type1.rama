type person = struct
     age: i32;
end

fn read(filename: str) -> i32
   let nathan: person = .{ age: 19 };
   nathan.age = 20;

   const name = filename;

   const a = (25 * 2) as i32;
   const b = a * 2;

   return b;
end