type string = [u8];

type Vec2<T> = struct {
     x: T,
     y: T,
}

type person = struct { age: i32; };

fn read(filename: string) i32 {
   var nathan: person = .{ age: 19 };
   nathan.age = 20;

   const name = filename;

   const a = (25 * 2) as i32;
   const b = a * 2;

   return b;
}