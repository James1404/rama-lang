type Num = i32;

type Option(T) = enum {
     Some: T;
     None;
};

type Player = struct {
     health: i32;
     alive: bool;
     name: string;
};

interface Animal {
   can_bark: bool;
   ty: bool;
}

type File = struct {
     path: string;
};

fn read(filename: string) Option(File, T) {
   const a = 25 * 2;
}

fn main() {
}